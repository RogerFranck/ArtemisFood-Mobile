import 'package:flutter/material.dart';

class RoundedButton extends StatelessWidget {
  const RoundedButton({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      //Bueno pues dejo de compartir pantalla
    );
  }
}